pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
               # sh 'dotnet build --configuration Release'
		  bash 'dotnet build --configuration Release'  
            }
        }  

	}
}
